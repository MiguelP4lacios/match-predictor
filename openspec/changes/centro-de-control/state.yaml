change: centro-de-control
phase: design
artifact_store: hybrid
execution_mode: auto
artifacts:
  exploration: false
  proposal: true
  specs: true
  design: true
  tasks: false
  verify_report: false
tasks_progress:
  completed: []
  pending: []
last_updated: 2026-06-11
notes: |
  Rediseño UX/UI profesional + observabilidad. Tema AUTO light+dark (script inline sin flash,
  ThemeContext, tokens CSS, Tailwind darkMode:class). 48 banderas reales de group_team (England/
  Scotland overrides, no son ISO2). 12 primitivas en src/ui/. Nav responsive (bottom-tab teléfono /
  top laptop). GroupCard reflow hidden md:table-cell + expandedRow (SIN scroll lateral). Obs:
  GET /api/v1/health/full + health_status.py (verdictos por umbral: odds age>10h stale/4-10 warn,
  credits<100 warn) + m8 (SyncLog rows_inserted+credits_remaining nullable) + capture escribe
  SyncLog en jobs.py (frontera de transacción, lección bug 2026-06-10). StatusBadge poll 60s +
  página Estado. ~160 tests frontend rompen por markup (mitigado: conservar lógica, reescribir render).
  Next: sdd-tasks.
