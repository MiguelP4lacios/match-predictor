change: dashboard-mvp
phase: apply
artifact_store: hybrid
execution_mode: interactive
artifacts:
  exploration: false
  proposal: true
  specs: true
  design: true
  tasks: true
  apply_progress: true
  verify_report: false
tasks_progress:
  completed: [Phase 1 (scaffolding+compose), Phase 2 (formatters+client 14 tests), Phase 3 (componentes 18 tests), Phase 4 (5 páginas+router 13 tests), Phase 5 (Dockerfile prod+nginx), Phase 6 (45/45 + build + smoke real con proxy)]
  pending: []
last_updated: 2026-06-10
notes: |
  Apply COMPLETO (2 agentes). 45/45 tests frontend (14 unit + 18 component + 13 integration),
  build tsc limpio (221KB bundle), backend 125/125 intacto. Smoke: HTML en 5173 + proxy vite
  /api -> api:8000 devolviendo JSON real de grupos. Desviaciones documentadas: volumen NOMBRADO
  frontend_node_modules (anónimo no se comparte entre run/up), formatStake sin símbolo $ (spec
  manda sobre design), AppRoutes export para testear con MemoryRouter. Riesgo para verify:
  GroupDetailPage sin integration test propio. Nota host: el IDE del usuario marca error en
  tsconfig (vite/client) porque node_modules vive en el volumen Docker, no en el host — esperado,
  no es un error real. Esperando aprobación para sdd-verify (interactivo).
